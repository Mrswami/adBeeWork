# Google OAuth Credentials
# Get these from https://console.cloud.google.com/ > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# GroupMe Integration (Get token from dev.groupme.com)
GROUPME_ACCESS_TOKEN=
GROUPME_DEFAULT_GROUP_ID= # Optional: Provide if you want auto-notifications to a specific group

# Firebase Setup (Optional for profile saving & history)
# Generate JSON at Console -> Project Settings -> Service Accounts
FIREBASE_PROJECT_ID=
FIREBASE_SERVICE_ACCOUNT_JSON= # Paste the entire JSON string on ONE line here

# Session secret (any random string)
SESSION_SECRET=adbee-secret-change-me

# Port (optional, defaults to 3000)
PORT=3000
